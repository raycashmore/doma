//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-svelte'],
  printWidth: 120,
  semi: true,
  singleQuote: true,
  trailingComma: 'none'
};

export default config;
